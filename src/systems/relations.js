// best matches for each personality
/*
class Relations {
    constructor()
    {
        this.relations = new Map();
    }

    addRelation(personality, personality2)
    {
        if(!this.relations.has(personality))
        {
            this.relations.set(personality, []);
        }
        this.relations.get(personality).push(personality2);
    }

    addMultipleRelations(personality, personalities)
    {
        if(!this.relations.has(personality))
        {
            this.relations.set(personality, []);
        }
        this.relations.get(personality).push(...personalities)
    }
}
    */